package com.proyecto360.health.ui.theme

import androidx.compose.ui.graphics.Color

/** Distinct palette for "compromiso del día" cards (warm amber, vs green posts). */
object CommitmentColors {
    val container = Color(0xFFFFF0D6)
    val onContainer = Color(0xFF4A3208)
    val accent = Color(0xFFB87514)
    val checkbox = Color(0xFFD4922A)
    val darkContainer = Color(0xFF3A2A12)
    val darkOnContainer = Color(0xFFFFE4B5)
    val darkAccent = Color(0xFFE6B35C)
}
